# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/

# --- Datos (nunca al repo: la DB vive en la VM / local) ---
*.sqlite
*.sqlite3
*.sqlite-wal
*.sqlite-shal
*.sqlite-shm
*.db

# --- Sitio generado (lo produce CI, no se commitea) ---
build/

# --- Secretos (repo público: nada de esto entra jamás) ---
.env
.env.*
*.pem
*.key
*.ppk
id_rsa*
id_ed25519*
*_rsa
*_ed25519
# Oracle Cloud / OCI
**/.oci/
oci_api_key*
*.oci
# credenciales genéricas
*credential*
*secret*
*.token
