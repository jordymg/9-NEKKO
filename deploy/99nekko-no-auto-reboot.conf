// NEKKO hardening (s8) — instalar en /etc/apt/apt.conf.d/99nekko-no-auto-reboot
//
// Sin reboot automático durante la ventana F2. Hay un reboot de kernel PENDIENTE
// (linux-image-6.17.0-1018-oracle, "System restart required") DIFERIDO a
// post-2026-08-03: reiniciar ahora cortaría la recolección de 14 días en curso.
// El kernel nuevo queda instalado en disco y se activa en el reboot de teardown.
Unattended-Upgrade::Automatic-Reboot "false";
Unattended-Upgrade::Automatic-Reboot-WithUsers "false";
